rodar no terminal:
g++ *.cpp -o ppm_compressao -O2

./ppm_compressao

compressao (ver binarios):
xxd -b teste_comprimido.bin

Novo comando (modularizado):

g++ src/*.cpp *.cpp -o ppm_compressao -O3 && ./ppm_compressao 3

terminal: chmod +x validar_compressao.sh
compilação: ./validar_compressao.sh